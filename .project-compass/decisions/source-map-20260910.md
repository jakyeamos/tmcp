# TMCP source-map reconciliation, 2026-09-10

The owner accepted packet runtime including storage and safety, with harvest and
distribution as neighbors, on 2026-09-09. The current request is to complete the
map before assessing modernization. Existing accepted purpose and stability
boundaries are preserved; no new behavior or promotion is authorized.

Packet construction, route resolution, receipt projection, persistence and safety
modules implement the packet runtime described by `docs/TMCP_PACKET_SPEC.md` and
`docs/ADAPTIVE_PACKET_RUNTIME.md`. Curated workflow skills are packet content,
including the release-readiness skill; that skill is not the TMCP release builder.
The README's stable package, template and tool lists remain separate authorities.

Evaluation plans, held-out fixtures, private behavioral-atom/H3 evaluation,
rollout metrics and workflow activation/promotion candidates belong to harvest
and evaluation. Experimental promotion stays deferred. Distribution owns release
and bootstrap validation scripts, not runtime acceptance of every packaged skill.

`source-map-review-20260910.json` records exact additions, source digests and
resolved cross-compass Python imports. Dependencies include consumers when shared
source changes; no duplicate ownership or prefix fallback establishes coverage.
The source map is broader than the existing regression oracles. Maturity and
acceptance claims are unchanged; further behavior-specific proof remains required.

Change-surface review: only Compass source ownership and provenance change. The
existing Compass row remains applicable; no CLI, runtime, installer, schema meaning
or release policy changes. No new installation or model-driven rewrite is approved.
