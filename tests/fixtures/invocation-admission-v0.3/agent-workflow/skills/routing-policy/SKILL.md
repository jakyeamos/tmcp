# Routing policy

Express routes as ordered predicates with a fail-closed fallback. Separate read-only advisory work from edits and external effects. A receipt must record route, artifacts, validation command, result, and unresolved blockers.
