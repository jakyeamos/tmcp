# TMCP object index

All entries are `status: stub` until a human-reviewed citation pass.

- `packet-runtime` — `tmcp_runtime/`, runtime launcher/scripts, MCP
  framing/server, `schemas/`, and `mcp-registry/`; `universe: live`; existing
  Compass coverage is incomplete until these paths are reconciled.
- `skill-harvest-recommendation` — `skills/` and skill evaluation/
  recommendation scripts; `universe: live`; does not own packet runtime.
- `release-distribution` — release/archive/composition scripts and distribution
  compatibility docs; `universe: live`; does not redefine runtime semantics.
- `experimental-integrations` — experimental workflows/tools and disabled
  AIOS adapter/storage integration; `universe: unknown`; no stable child
  Compass is implied.
- `support-and-examples` — assets, examples, and tests; `universe: live`; does
  not automatically become a product child.

